name: Release
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v1.0.0)'
        required: true
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      # Create the GitHub Release first so a missing/invalid NPM_TOKEN
      # cannot leave the Releases section empty. npm publish runs after.
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          generate_release_notes: true

      # Publish to npm so hosts can `npx wikey-wallet-mcp`.
      # Requires the NPM_TOKEN secret to be set on the repo.
      # If the token is absent this step fails (job goes red) but the
      # GitHub Release above is already published.
      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
